import React from 'react';
import './MainContent.css';

const MainContent = () => {
    return (
        <div className="main-content">
            <h2>Welcome to Thread</h2>
            {/* Add content here, e.g., posts, feed, etc. */}
        </div>
    );
};

export default MainContent;
